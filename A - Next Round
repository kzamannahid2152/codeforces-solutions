#include<iostream>
using namespace std;
int main(){
    int n, k,array[50],count=0;
    cin>>n>>k;
    for(int i=0; i<n; i++){
        cin>>array[i];
    }
    for(int i=0; i<n; i++){
        if(array[i] > 0 && array[i] >= array[k-1])
            count++;
    }
    cout<<count<<endl;

}
